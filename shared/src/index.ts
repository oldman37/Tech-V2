/**
 * @mgspe/shared-types
 * 
 * Shared TypeScript types for MGSPE frontend and backend
 * Ensures type consistency across the entire application
 */

// Core domain types
export * from './types';

// API request/response types
export * from './api-types';
