# Environment Configuration
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/tech_v2"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="1h"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Microsoft Entra ID (Azure AD)
ENTRA_TENANT_ID="your-tenant-id"
ENTRA_CLIENT_ID="your-client-id"
ENTRA_CLIENT_SECRET="your-client-secret"
REDIRECT_URI="http://localhost:3000/api/auth/callback"
ENTRA_ADMIN_GROUP_ID="admin-group-object-id"
# Finance Director Entra group — only members can approve POs at the Finance Director stage
ENTRA_FINANCE_DIRECTOR_GROUP_ID="finance-director-group-object-id"
# Director of Schools Entra group — only members can approve POs at the DoS stage
ENTRA_DIRECTOR_OF_SCHOOLS_GROUP_ID="director-of-schools-group-object-id"
# Food Services PO Entry Entra group — members can enter food service POs
ENTRA_FOOD_SERVICES_PO_ENTRY_GROUP_ID="food-services-po-entry-group-object-id"

# Email Configuration (if needed)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="your-email@example.com"
SMTP_PASS="your-email-password"
SMTP_FROM="Tech Department <tech@example.com>"

# Application base URL (used for deep links in emails)
APP_URL="https://your-app-domain.com"

# File Upload
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=10485760

# Logging
LOG_LEVEL="info"

# CORS
CORS_ORIGIN="http://localhost:5173"
